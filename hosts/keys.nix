# Public keys allowed to ssh into every NixOS host as the primary user.
# Password auth is off, so this list must not be empty (asserted).
{
  admin = [
    # "ssh-ed25519 AAAA... hackerman@mbp"
  ];
}
